# UNIDOCK

Monorepo for Unidock apps.

- App: `apps/unidock` (imported from floating-dock)
- Package manager: pnpm

Quick start:

```bash
cd C:\Users\Administrator.AVENTADOR\UniDock\UNIDOCK
pnpm install
pnpm -w -C apps/unidock dev
```
